{
    dishes: render(partial: 'dish', collection: @dishes),
    paging: {
        total_pages: @dishes.total_pages,
        total_count: @dishes.total_count,
        current_page: @dishes.current_page
    }
}